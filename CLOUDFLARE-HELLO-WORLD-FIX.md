# Cloudflare Pages 显示 "Hello world" 问题修复

## 🚨 问题

网页可以打开，但只显示了 "Hello world"，而不是你的 React 应用。

## 🔍 可能的原因

1. **构建输出目录不正确**
2. **构建没有正确完成**
3. **Cloudflare Pages 使用了默认页面**
4. **index.html 文件路径问题**

## ✅ 解决方案

### 步骤 1: 检查 Cloudflare Pages 构建日志

1. 进入 Cloudflare Dashboard
2. 进入你的 Pages 项目
3. 点击 **Deployments** 标签
4. 查看最新的部署日志
5. 检查：
   - ✅ Build 是否成功
   - ✅ 是否生成了 `dist/` 目录
   - ✅ 是否有错误信息

### 步骤 2: 验证构建输出目录

在 Cloudflare Pages 项目设置中：

1. 进入 **Settings** → **Builds & deployments**
2. 确认 **Build output directory** 设置为：`dist`
3. 如果不对，修改为 `dist` 并保存

### 步骤 3: 检查构建命令

确保构建命令正确：

```
Build command: npm run build
```

### 步骤 4: 验证 dist 目录内容

构建成功后，`dist/` 目录应该包含：
- `index.html` - 主 HTML 文件（应该引用编译后的 JS）
- `assets/` - 包含编译后的 JS 和 CSS 文件

### 步骤 5: 检查 index.html

构建后的 `dist/index.html` 应该类似这样：

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ScanWhat - Image to Text</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- ... -->
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/assets/index-xxxxx.js"></script>
  </body>
</html>
```

**注意**：脚本路径应该是 `/assets/index-xxxxx.js`，而不是 `/index.tsx`

### 步骤 6: 重新部署

1. 在 Cloudflare Pages Dashboard 中
2. 进入 **Deployments** 标签
3. 找到最新的部署
4. 点击 **Retry deployment** 重新部署

或者：

1. 推送一个新的提交到 GitHub
2. Cloudflare Pages 会自动触发新的部署

## 🔧 如果问题仍然存在

### 检查 1: 查看部署的文件

1. 在 Cloudflare Pages Dashboard 中
2. 进入 **Deployments** 标签
3. 点击最新的部署
4. 查看部署的文件列表
5. 确认 `index.html` 和 `assets/` 目录存在

### 检查 2: 查看浏览器控制台

1. 打开你的网站
2. 按 F12 打开开发者工具
3. 查看 **Console** 标签
4. 查看是否有错误信息
5. 查看 **Network** 标签，检查资源是否加载成功

### 检查 3: 验证构建输出

在本地测试构建：

```bash
npm run build
ls -la dist/
```

应该看到：
- `index.html`
- `assets/` 目录（包含 JS 文件）

### 检查 4: 检查 Cloudflare Pages 设置

确保以下设置正确：

```
Framework preset: Vite 或 None
Build command: npm run build
Build output directory: dist
Root directory: /
Deploy command: echo "Deployment handled by Cloudflare Pages"
```

## 🐛 常见问题

### 问题 1: 显示 Cloudflare 默认页面

**原因**：构建输出目录不正确或构建失败

**解决**：
- 检查构建日志
- 确认 Build output directory 为 `dist`
- 重新部署

### 问题 2: 显示空白页面

**原因**：JavaScript 文件没有正确加载

**解决**：
- 检查浏览器控制台错误
- 检查 Network 标签，确认 JS 文件加载成功
- 检查 `index.html` 中的脚本路径

### 问题 3: 404 错误

**原因**：路由配置问题

**解决**：
- 检查 `_redirects` 文件是否在 `dist/` 目录
- 确认 Cloudflare Pages 支持 SPA 路由

## ✅ 验证修复

修复后，你应该看到：

1. ✅ 网站显示 "ScanWhat" 标题
2. ✅ 有 "Take Photo" 和 "Upload Image" 按钮
3. ✅ 浏览器控制台没有错误
4. ✅ Network 标签显示所有资源加载成功

## 📝 调试技巧

1. **查看构建日志**：在 Cloudflare Pages Dashboard 中查看完整的构建日志
2. **检查部署文件**：确认所有文件都正确部署
3. **浏览器控制台**：查看 JavaScript 错误
4. **Network 标签**：检查资源加载情况

