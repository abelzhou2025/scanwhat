# Cloudflare Pages 快速修复指南

## 🚨 当前错误

```
✘ [ERROR] It looks like you've run a Workers-specific command in a Pages project.
For Pages, please run `wrangler pages deploy` instead.
```

## ✅ 快速修复（3 步）

### 步骤 1: 进入 Cloudflare Dashboard
1. 访问 https://dash.cloudflare.com/
2. 登录你的账户
3. 进入 **Workers & Pages** → 选择你的项目

### 步骤 2: 修改构建设置
1. 点击项目名称进入项目详情
2. 点击 **Settings** 标签
3. 滚动到 **Builds & deployments** 部分
4. 找到以下字段并修改：
   - **Deploy command**: **完全删除/清空**（留空，不要填写 `/` 或任何内容）
   - **Version command**: **完全删除/清空**（留空，不要填写 `npx wrangler versions upload` 或任何内容）

### 步骤 3: 验证其他设置
确保以下设置正确：

- ✅ **Build command**: `npm run build`
- ✅ **Build output directory**: `dist`
- ✅ **Root directory**: `/` (留空或 `/`)
- ✅ **Deploy command**: **(留空，不要设置任何内容)**
- ✅ **Version command**: **(留空，不要设置任何内容)**

### 步骤 4: 保存并重新部署
1. 点击 **Save** 保存设置
2. 点击 **Retry deployment** 或等待下一次 Git 提交自动触发部署

## 🔑 设置环境变量（如果还没设置）

1. 在项目设置中，进入 **Settings** → **Environment variables**
2. 点击 **Add variable**
3. 添加：
   - **Variable name**: `GEMINI_API_KEY`
   - **Value**: 你的 Google Gemini API 密钥
   - **Environment**: 选择 **Production** 和 **Preview**（或根据需要选择）
4. 点击 **Save**
5. **重要**：环境变量更改后需要重新部署才能生效

## ✅ 验证部署成功

部署成功后，你应该看到：
- ✅ Build 成功（绿色勾）
- ✅ Deploy 成功（绿色勾）
- ✅ 网站可以访问（`.pages.dev` 域名）

## 🧪 测试 API

部署成功后，测试 OCR API 是否工作：

1. 访问你的网站（`.pages.dev` 域名）
2. 上传一张包含文字的图片
3. 检查是否能成功提取文字

如果 API 不工作，检查：
- 环境变量是否已设置
- 是否已重新部署（环境变量更改后需要重新部署）
- Cloudflare Pages Functions 日志（在 Dashboard 中查看）

## 📝 为什么会出现这个错误？

Cloudflare Pages 有两种部署方式：
1. **自动部署**（推荐）：Cloudflare 自动检测构建输出并部署
2. **手动部署**：使用 `wrangler pages deploy` 命令

如果你设置了 `npx wrangler deploy`，这是 Workers 的命令，不是 Pages 的，所以会报错。

**解决方案**：移除自定义部署命令，让 Cloudflare 自动处理。

