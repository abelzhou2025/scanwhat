# Cloudflare Pages 创建步骤

## 🚨 当前页面

你看到的是 "Ship something new" 页面，这是创建新项目的初始页面。

## ✅ 创建 Pages 项目的步骤

### 步骤 1: 选择 "Continue with GitHub"

1. 点击 **"Continue with GitHub"** 按钮（左上角，有绿色圆点）
2. 这会打开 GitHub 授权页面
3. 授权 Cloudflare 访问你的 GitHub 仓库

### 步骤 2: 选择仓库

授权后，你会看到你的 GitHub 仓库列表：

1. 找到并选择 **`abelzhou2025/sacnwhat`** 仓库
2. 点击仓库名称

### 步骤 3: 配置构建设置

选择仓库后，Cloudflare 会显示配置页面。在这里配置：

#### 项目名称
- **Name**: `scanwhat`（或你喜欢的名称）

#### 构建设置
- **Framework preset**: 选择 `Vite` 或 `None`
- **Build command**: `npm run build`
- **Build output directory**: `dist` ← **重要！**
- **Root directory**: `/` (留空)

#### 分支设置
- **Production branch**: `main`
- **Builds for non-production branches**: 可以启用（用于预览）

### 步骤 4: 保存并部署

1. 检查所有设置
2. 点击 **"Save and Deploy"** 或 **"Deploy site"**
3. Cloudflare 会自动：
   - 克隆仓库
   - 安装依赖
   - 运行构建命令
   - 部署到 `.pages.dev` 域名

## 🔍 重要提示

### 关于项目类型

Cloudflare 现在可能不再明确显示 "Pages" vs "Workers" 的选择。当你：
- 选择 **"Continue with GitHub"** 并连接仓库
- 配置 **Build output directory** 为 `dist`
- 部署静态文件

Cloudflare 会自动识别这是一个 **Pages** 项目。

### 如果看到 "Start with Hello World!"

- 这个选项是用于创建新的 Workers 项目
- **不要选择这个**，因为你需要连接现有的 GitHub 仓库

### 如果看到 "Upload your static files"

- 这个选项是用于手动上传静态文件
- **不要选择这个**，因为你需要从 Git 仓库自动部署

## ✅ 正确的流程

```
1. 点击 "Continue with GitHub"
   ↓
2. 授权 GitHub 访问
   ↓
3. 选择仓库 abelzhou2025/sacnwhat
   ↓
4. 配置构建设置（Build output directory: dist）
   ↓
5. 保存并部署
   ↓
6. 获得 .pages.dev 域名
```

## 🔑 环境变量设置

部署成功后，设置环境变量：

1. 进入项目 → **Settings** → **Environment variables**
2. 点击 **"Add variable"**
3. 添加：
   - **Variable name**: `GEMINI_API_KEY`
   - **Value**: 你的 Google Gemini API 密钥
   - **Environment**: 选择 **Production** 和 **Preview**
4. 点击 **Save**
5. **重要**：环境变量更改后需要重新部署才能生效

## 📋 完整配置检查清单

创建项目时，确保：

- ✅ 选择了 "Continue with GitHub"（不是其他选项）
- ✅ 选择了正确的仓库：`abelzhou2025/sacnwhat`
- ✅ Framework preset: `Vite` 或 `None`
- ✅ Build command: `npm run build`
- ✅ **Build output directory: `dist`** ← 最重要！
- ✅ Root directory: `/` (留空)
- ✅ Production branch: `main`

## 🐛 常见问题

### 问题 1: 没有看到 "Build output directory" 字段

**原因**：可能选择了错误的选项（如 "Start with Hello World!"）

**解决**：
- 确保选择了 "Continue with GitHub"
- 确保选择了你的 GitHub 仓库
- 在配置页面应该能看到 "Build output directory" 字段

### 问题 2: 部署后显示 "Hello world"

**原因**：Build output directory 设置错误或构建失败

**解决**：
- 检查 Build output directory 是否为 `dist`
- 查看部署日志，确认构建成功
- 确认 `dist/` 目录包含 `index.html` 和 `assets/` 目录

### 问题 3: 找不到仓库

**原因**：GitHub 授权不完整或仓库不存在

**解决**：
- 重新授权 GitHub 访问
- 确认仓库名称正确：`abelzhou2025/sacnwhat`
- 确认仓库是公开的或已授权 Cloudflare 访问

## ✅ 验证部署成功

部署成功后，你应该看到：

1. ✅ 项目创建成功
2. ✅ 有一个 `.pages.dev` 域名（例如：`scanwhat-xxxxx.pages.dev`）
3. ✅ 网站显示 "ScanWhat" 应用（不是 "Hello world"）
4. ✅ 可以访问网站并测试功能

## 📝 下一步

部署成功后：

1. **测试网站**：访问 `.pages.dev` 域名
2. **设置环境变量**：添加 `GEMINI_API_KEY`
3. **测试 OCR 功能**：上传图片测试文字提取
4. **检查 Functions**：确认 `/api/ocr` 路由工作

