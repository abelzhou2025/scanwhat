# Cloudflare Pages 配置修复

## 🚨 当前配置问题

你的当前配置：
```
Build command: npm run build          ✅ 正确
Deploy command: /                     ❌ 错误！应该留空
Version command: npx wrangler versions upload  ❌ 错误！应该留空
Root directory: /                     ✅ 正确
```

## ✅ 正确的配置

应该是：
```
Build command: npm run build          ✅
Deploy command: (留空)                ✅ 不要填写任何内容
Version command: (留空)               ✅ 不要填写任何内容
Root directory: /                    ✅
Build output directory: dist          ✅
```

## 🔧 修复步骤

### 1. 进入 Cloudflare Dashboard
1. 访问 https://dash.cloudflare.com/
2. 进入 **Workers & Pages** → 选择你的项目

### 2. 修改设置
1. 点击项目名称进入项目详情
2. 点击 **Settings** 标签
3. 滚动到 **Builds & deployments** 部分
4. 找到以下字段：

#### Deploy command
- **当前值**: `/`
- **应该改为**: **完全删除，留空**
- **操作**: 点击字段，删除所有内容，留空

#### Version command
- **当前值**: `npx wrangler versions upload`
- **应该改为**: **完全删除，留空**
- **操作**: 点击字段，删除所有内容，留空

#### Build command
- **当前值**: `npm run build`
- **应该改为**: 保持不变 ✅

#### Root directory
- **当前值**: `/`
- **应该改为**: 保持不变 ✅

#### Build output directory
- **确保设置为**: `dist`
- 如果看不到这个字段，可能需要展开更多选项

### 3. 保存设置
1. 滚动到页面底部
2. 点击 **Save** 按钮
3. 等待保存完成

### 4. 重新部署
1. 点击 **Deployments** 标签
2. 找到最新的部署（应该是失败的）
3. 点击 **Retry deployment** 按钮
4. 或者等待下一次 Git 提交自动触发部署

## 📝 为什么这些字段应该留空？

### Deploy command
- **错误理解**: 以为需要设置部署命令
- **实际情况**: Cloudflare Pages 会自动检测 `dist/` 目录并自动部署
- **如果设置**: 会尝试运行你指定的命令，导致错误

### Version command
- **错误理解**: 以为需要设置版本命令
- **实际情况**: `npx wrangler versions upload` 是用于 **Cloudflare Workers** 的，不是 Pages
- **如果设置**: 会在 Pages 项目中运行 Workers 命令，导致错误

## ✅ 验证修复

修复后，部署应该：
1. ✅ 构建成功（`npm run build`）
2. ✅ 自动检测 `dist/` 目录
3. ✅ 自动部署静态文件
4. ✅ 自动识别 `functions/` 目录中的 Functions
5. ✅ 部署成功，网站可访问

## 🔑 别忘了设置环境变量

如果还没设置环境变量：

1. 在项目设置中，进入 **Settings** → **Environment variables**
2. 点击 **Add variable**
3. 添加：
   - **Variable name**: `GEMINI_API_KEY`
   - **Value**: 你的 Google Gemini API 密钥
   - **Environment**: 选择 **Production** 和 **Preview**
4. 点击 **Save**
5. **重要**: 环境变量更改后需要重新部署才能生效

## 🧪 测试

部署成功后：
1. 访问你的网站（`.pages.dev` 域名）
2. 上传一张包含文字的图片
3. 检查是否能成功提取文字

如果 API 不工作，检查：
- 环境变量是否已设置
- 是否已重新部署（环境变量更改后需要重新部署）
- Cloudflare Pages Functions 日志（在 Dashboard 中查看）

